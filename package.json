{
  "name": "final-project-start2impact",
  "version": "1.0.0",
  "description": "Workspace del progetto finale Start2Impact",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "start:server": "npm run start --workspace server"
  }
}
